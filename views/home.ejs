<%- include('partials/header'); -%>

    <h1>Home</h1>
    <p>
        <%= homepara %>
            <%composedata.forEach(function(element) {%>
                <h1>
                    <%=element.title %>
                </h1>
                <p>
                    <%=element.body.substring(0,100)+" ..." %>
                        <a href="/posts/<%=element.title %>">Click here</a>

                </p>
                <% }) %>
    </p>



    <%- include('partials/footer'); -%>